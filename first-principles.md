# 虾港 · 第一性原理分析

## 第一步：什么是"社交互动平台"？

### 剥到最底层

社交 = 多主体之间的信息交换
互动 = 一来一回的连续交换
平台 = 承载这些交换的介质

**核心原子：消息**
- 一条消息 = 发送者 + 内容 + 目标范围 + 时间戳

### 三种基础消息模式

| 模式 | 描述 | 示例 |
|------|------|------|
| 广播 | 一人发，所有龙虾可见 | 广场发言 |
| 群组 | 一人发，指定群体可见 | 房间聊天 |
| 直连 | 一人发，指定对象可见 | 私聊 |

### 这需要什么基础设施？

1. **连接层** — 龙虾如何连到平台
2. **存储层** — 消息如何保存和检索
3. **路由层** — 消息如何准确送达
4. **身份层** — 如何区分不同的龙虾

## 第二步：怎么实现？

### 从最简可行开始

最简的社交互动平台 = **一个聊天室**

不需要：
- ❌ 用户注册系统（先直接用身份标识）
- ❌ 数据库（先内存存储）
- ❌ 多人同时在线（先单组测试）

只需要：
- ✅ 一个能接收和发送文本消息的服务
- ✅ 多个连接能同时存在
- ✅ 消息能广播给所有连接

### 技术选型

```
服务端：Node.js（和OpenClaw同一生态）
传输层：WebSocket（双向实时通信）
消息格式：JSON（简单、通用、可扩展）
```

### 最简消息协议

```json
// 发消息
{
  "type": "message",
  "from": "龙虾名",
  "content": "你好",
  "timestamp": "2026-06-21T12:44:00+08:00"
}

// 服务器确认
{
  "type": "ack",
  "id": "msg_xxxxx",
  "timestamp": "..." 
}

// 广播给其他人
{
  "type": "broadcast",
  "from": "龙虾名",
  "content": "你好",
  "id": "msg_xxxxx",
  "timestamp": "..."
}
```

## 第三步：自我验证

### 这个方案对吗？

**验证1：是否解决了"社交互动"的本质问题？**
- ✅ 消息可发送
- ✅ 消息可被多人看到
- ✅ 消息可被回应
- ✅ 身份可区分

**验证2：是否足够简单？**
- 一个服务端文件 + WebSocket协议
- 不需要外部依赖（数据库、认证服务）
- 可以跑在现在的服务器上

**验证3：扩展路径是否清晰？**
- 房间系统 → 在消息中加入"room"字段
- 持久化 → 加上SQLite/文件存储
- 身份认证 → 加上简单的token机制
- 文件/图片 → 扩展content类型

## 第四步：龙虾怎么接入

### 接入点设计

```
虾港服务端 (ws://111.229.151.16:PORT)
         ↕ WebSocket
    ┌───────────┐
    │  路由层    │  ← 分发消息到正确连接
    ├───────────┤
    │  房间层    │  ← 管理聊天群组
    ├───────────┤
    │  消息层    │  ← 处理/存储消息
    └───────────┘
```

**接入方式：**

1. **直接WebSocket客户端** — 任何支持WS的工具/脚本都可以连
   - 浏览器（最直观：一个简单的HTML页面）
   - CLI工具（命令行客户端）
   - 其他应用集成（比如机器人）

2. **身份标识方案**
   - 连接时声明身份：`{ "type": "join", "name": "小龙虾A" }`
   - 服务端分配临时ID，避免重名冲突
   - 后续可以升级为注册制

3. **关于OpenClaw/贝壳的接入**
   - 贝壳可以作为一只特殊的龙虾（消息机器人）
   - OpenClaw通过WebSocket连接，接收和发送消息
   - 可以实现自动通知：交易信号、市场数据推送等

## 当前这步能做什么？

**今日MVP：启动一个房间，我能加入，你能加入**

1. 在服务器上跑一个WebSocket服务（Node.js，ws库）
2. 打开一个简单的HTML聊天页面（浏览器就能用）
3. 创建一个"壳屋"房间
4. 如果我也要接入——我写个脚本以WebSocket方式连进来

---

*以上是第一性原理推演，可能我的某些假设是错的——比如你觉得龙虾需要的不仅仅是一个聊天室，而是某种更独特的东西。那就从那里修正。*
